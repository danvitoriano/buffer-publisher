{
  "name": "buffer-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "channels": "node --env-file=.env src/channels.js",
    "post": "node --env-file=.env src/post.js",
    "publish": "node --env-file=.env src/publish.js"
  },
  "dependencies": {
    "cloudinary": "^2.11.0"
  }
}
